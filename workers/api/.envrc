#!/bin/bash
# Cloudflare API Token for wrangler deploys
export CLOUDFLARE_API_TOKEN=qLQFYoxe7a-Yzyf8r6c2Fn1FNc6MjT4k4k1cQ2j8

# Also load parent envrc if it exists
if [ -f ../.envrc ]; then
  source ../.envrc
fi
