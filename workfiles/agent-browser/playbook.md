# Agent Browser Playbook

## 2026-05-13 Platform Audit Route Sweep

Session: `ciutatis-audit`

Server:
- `http://127.0.0.1:3100`
- Dev command used an isolated local instance with `PAPERCLIP_HOME=.paperclip-local-dev` and `PAPERCLIP_INSTANCE_ID=codex-dev`.

Reusable steps:
1. Clear browser diagnostics before each route with `agent-browser --session ciutatis-audit console --clear` and `agent-browser --session ciutatis-audit errors --clear`.
2. Set desktop viewport with `agent-browser --session ciutatis-audit set viewport 1440 900`.
3. Open each operator route, wait 2000 ms, then evaluate:
   - `document.documentElement.scrollWidth > document.documentElement.clientWidth`
   - visible text for first-party API/route failures
   - `agent-browser --session ciutatis-audit errors`
   - `agent-browser --session ciutatis-audit console`
4. Set mobile viewport with `agent-browser --session ciutatis-audit set viewport 390 844`.
5. Repeat the same checks for public marketing routes.

Routes verified:
- `/COD/dashboard`
- `/COD/inbox`
- `/COD/requests`
- `/COD/projects`
- `/COD/agents/all`
- `/COD/search`
- `/COD/institution/settings`
- `/COD/institution/settings/environments`
- `/instance/settings/overview`
- `/instance/settings/tenants`
- `/instance/settings/cloudflare`
- `/instance/settings/plugins`
- `/`
- `/en/platform`
- `/es/procesos`
- `/en/contact`

Outcomes:
- All verified routes rendered without browser page errors.
- No verified route had horizontal overflow at the tested viewport.
- `/api/companies/:companyId/join-requests?status=pending_approval` now returns `200 []` locally instead of being intercepted by plugin routing.
- `/api/instance/settings/admin-overview`, `/api/instance/settings/tenants`, `/api/instance/settings/tenant-provisioning`, and `/api/instance/settings/cloudflare` now return 200s for the instance panels.
- `/en/contact` now enters the public marketing site and scrolls to the existing contact section.

Screenshots:
- `workfiles/agent-browser-dashboard-desktop.png`
- `workfiles/agent-browser-instance-overview-desktop.png`
- `workfiles/agent-browser-contact-mobile.png`

## 2026-05-13 Live Deploy Verification

Session: `ciutatis-deploy`

Commit: `5d22000f` (`Audit and wire platform panels`)

Reusable steps:
1. Set mobile viewport with `agent-browser --session ciutatis-deploy set viewport 390 844`.
2. Open `https://ciutatis.com/en/contact`, wait 1000 ms, then evaluate route, title, horizontal overflow, contact-section visibility, scroll position, and loaded asset.
3. Clear browser diagnostics with `agent-browser --session ciutatis-deploy errors --clear` and `agent-browser --session ciutatis-deploy console --clear`.
4. Set desktop viewport with `agent-browser --session ciutatis-deploy set viewport 1440 900`.
5. Open `https://admin.ciutatis.com/instance/settings/overview`, wait 1000 ms, then evaluate route, title, horizontal overflow, root mounting, visible text, and loaded asset.
6. Clear browser diagnostics again after the admin check.

Routes verified:
- `https://ciutatis.com/en/contact`
- `https://admin.ciutatis.com/instance/settings/overview`

Outcomes:
- Public contact route returned `https://ciutatis.com/en/contact`, title `Ciutatis`, asset `/assets/index-fnt1jvJo.js`, no horizontal overflow, and the contact section was visible after route-driven scroll.
- Admin settings route loaded the same deployed asset and redirected unauthenticated access to `https://admin.ciutatis.com/auth`, which is expected for the authenticated deployment mode.
- After redeploying `ciutatis-admin-ui` with the admin-specific `_redirects` generated by `scripts/prepare-admin-pages-dist.mjs`, `https://admin.ciutatis.com/` returned the app shell directly and the live status snapshot reported `overall=operational`.

## 2026-05-13 Argentina Tenant Routing Check

Session: default `agent-browser`

Local servers:
- UI: `http://localhost:4173`
- API: `http://127.0.0.1:3100`
- API used an isolated temp `PAPERCLIP_HOME` and `PAPERCLIP_INSTANCE_ID=browser`.

Reusable steps:
1. Start the UI with `pnpm --filter @paperclipai/ui dev`.
2. Start the API from `server/` with a temporary `PAPERCLIP_HOME`, `PAPERCLIP_CONFIG`, `PAPERCLIP_MIGRATION_PROMPT=never`, and `PAPERCLIP_MIGRATION_AUTO_APPLY=true`.
3. Open `http://localhost:4173/instance/settings/tenants`.
4. Verify route preview, seeded tenant row, and selected tenant detail include `/ar/municipio/7000-tandil`.
5. Set mobile viewport with `agent-browser set viewport 390 844` and repeat the same text checks.

Routes verified:
- `http://localhost:4173/instance/settings/tenants`

Outcomes:
- The tenant create form defaulted to Tandil City with jurisdiction type `municipio`, postal code `7000`, and parent `Provincia de Buenos Aires`.
- The route preview, seeded tenant row, public route, dispatcher key, and dispatch script all used the new Argentina path model.
- Desktop and mobile checks rendered the same route text without horizontal text loss in the captured page text.

## 2026-05-13 Production Tandil Route Check

Session: default `agent-browser`

Commands:
1. Open `https://ciutatis.com/ar/municipio/7000-tandil`.
2. Wait for network idle.
3. Read current URL and title.
4. Capture an interactive snapshot.

Routes verified:
- `https://ciutatis.com/ar/municipio/7000-tandil`
- `https://ciutatis.com/ar/municipio/7000-tandil/__tenant/health`

Outcomes:
- Browser stayed on `https://ciutatis.com/ar/municipio/7000-tandil`.
- Title was `Tandil City - Ciutatis`.
- Snapshot showed the tenant page and no interactive fallback shell controls.
- Health returned `ok: true` with tenant id `da100700-4f2a-4b7b-9000-000000007000`, path prefix `/ar/municipio/7000-tandil`, and D1/KV/R2 bindings present.

## 2026-05-13 Tandil Public Page Map Check

Session: default `agent-browser`

Commands:
1. Open `https://ciutatis.com/ar/municipio/7000-tandil`.
2. Wait for network idle and verify title, map status, Leaflet presence, loaded OSM tiles, boundary SVG paths, and horizontal overflow.
3. Click breadcrumb links for province, country, and partido; after each click wait for `Boundary loaded` and verify URL, active breadcrumb, heading, and boundary path count.
4. Set mobile viewport with `agent-browser set viewport 390 844`.
5. Reopen `https://ciutatis.com/ar/municipio/7000-tandil` and verify no horizontal overflow, visible form, and loaded municipal boundary.
6. Clear and inspect browser diagnostics with `agent-browser errors` and `agent-browser console`.

Routes verified:
- `https://ciutatis.com/ar/municipio/7000-tandil`
- `https://ciutatis.com/ar/partido/7000-tandil`
- `https://ciutatis.com/ar/provincia/buenos-aires`
- `https://ciutatis.com/ar/nacion/argentina`

Outcomes:
- The production route rendered `Tandil Public Civic Portal - Ciutatis` instead of the empty tenant placeholder.
- Leaflet loaded, the OSM tile layer rendered, and the Georef municipal polygon was present.
- Breadcrumb navigation updated the browser URL and map boundary in place for municipio, partido, province, and country scopes.
- Desktop and mobile checks reported no horizontal overflow.
- After fixing the Leaflet CSS SRI hash and redeploying, browser page errors and console output were clean on a fresh production load.

## 2026-06-08 Superparser Portal Verification

Session: default `agent-browser`

Local servers:
- Portal: `http://localhost:4174`
- Superparser service: `http://localhost:8080`

Reusable steps:
1. Start the service container with `docker run --rm -p 8080:8080 superparser-service:local`.
2. Start the portal with `pnpm --filter @ciutatis/superparser-portal dev`.
3. Open `http://localhost:4174/?verify=4` and wait for network idle.
4. Fill Drive source id `drive-live-budget` and Drive demo content `Planning Board approved a $480,000 emergency road repair contract on June 7, 2026. Public Works owns delivery.`
5. Click `Ingest Drive source`, then resnapshot before further interaction.
6. Search for `emergency road repair owner`.
7. Evaluate `#classification-badge`, `#flat-text`, `#extraction-list .extraction`, and `#search-results .result`.

Outcomes:
- Live service ingestion returned a succeeded job row in the portal queue.
- Document review rendered classification `infrastructure`, flat text for the just-ingested Drive source, grounded `$480,000` and `Public Works` spans, and no offline fallback row.
- Semantic search returned the ingested `drive-source.txt` document with the road repair text.
- Screenshot saved at `workfiles/agent-browser/superparser-portal-verification.png`.
